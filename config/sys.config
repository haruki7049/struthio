[{struthio, [{storage_limit_pct, 85},  %% Reject new events if disk usage >= 85%
             {default_ttl, 604800},  %% Default 7 days expiration (in seconds)
             {cleanup_interval, 60000}  %% Run garbage collection every 60 seconds
            ]}].
